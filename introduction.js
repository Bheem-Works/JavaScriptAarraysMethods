	// In this javascript file. I am going to write the all arrays methods. This arrays methods is going to be the useful in the projects.
	// There are variouse of arrays methods which have it's own works. So i am also going to some exercise proble by using the arrays methods.



	// The summary Content of the Useful Arrays Methods :-

	// toString();
	// join();
	// concat();
	// splice();
	// indexOf();
	// lastIndexOf();
	// forEach();
	// map();
	// filter();
	// reduce();
	// some();
	// every();
	// flat();
	// find();
	// findIndex();
	// sort();
